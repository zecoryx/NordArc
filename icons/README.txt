// Utility script to generate a simple icon if needed
// This file is not used by the extension directly but serves as documentation
// Icons are expected in the icons/ folder.
// Since we are in a text-only environment, we update manifest to rely on user providing icons
// or we can use a base64 placeholder if strictly necessary.
